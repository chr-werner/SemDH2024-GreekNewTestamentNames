# na28-crawler

A simple form (no annotations) of the NA28 can be found at `https://www.die-bibel.de/en/bible/NA28/`. From where it can be scraped by adding `{BOOK}.{CHAPTER}` (e.g. `MAT.5`) to the URL.